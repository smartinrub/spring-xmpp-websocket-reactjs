import { connect } from "react-redux";
import AddContactBox from "../components/add_contact_box/AddContactBox";

const AddContactBoxContainer = connect()(AddContactBox);

export default AddContactBoxContainer;
